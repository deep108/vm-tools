MacOS provisioning
* Keyboard repeat rate
* Close windows settings for iTerm session restore
* Clean up dock items
* Add volume controls to menubar
* Set default git branch to main: git config --global init.defaultBranch main
* codex-named shell command
